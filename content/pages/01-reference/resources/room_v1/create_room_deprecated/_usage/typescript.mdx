```typescript CreateRoomDeprecated.ts
import { HathoraCloud } from "@hathora/cloud-sdk-typescript";
import { CreateRoomParams, Region } from "@hathora/cloud-sdk-typescript/dist/models/components";
import { CreateRoomDeprecatedRequest } from "@hathora/cloud-sdk-typescript/dist/models/operations";

async function run() {
  const sdk = new HathoraCloud({
    hathoraDevToken: "<YOUR_BEARER_TOKEN_HERE>",
    appId: "app-af469a92-5b45-4565-b3c4-b79878de67d2",
  });
const createRoomParams: CreateRoomParams = {
  roomConfig: "{\"name\":\"my-room\"}",
  region: Region.Seattle,
};
const appId: string = "app-af469a92-5b45-4565-b3c4-b79878de67d2";
const roomId: string = "2swovpy1fnunu";

  const res = await sdk.roomV1.createRoomDeprecated(createRoomParams, appId, roomId);

  if (res.statusCode == 200) {
    // handle response
  }
}

run();
```
