
import WithStatusBar from '@/src/components/WithStatusBar';

<WithStatusBar statusItems={[["Status", "201"], ["Content-Type", "application/json"]]}>
  <CH.Code>
  ```typescript CreateRoom.ts
  import { HathoraCloud } from "@hathora/cloud-sdk-typescript";
import { CreateRoomParams, Region } from "@hathora/cloud-sdk-typescript/dist/models/components";
import { CreateRoomRequest } from "@hathora/cloud-sdk-typescript/dist/models/operations";

async function run() {
  const sdk = new HathoraCloud({
    hathoraDevToken: "<YOUR_BEARER_TOKEN_HERE>",
    appId: "app-af469a92-5b45-4565-b3c4-b79878de67d2",
  });
const createRoomParams: CreateRoomParams = {
  roomConfig: "{\"name\":\"my-room\"}",
  region: Region.London,
};
const appId: string = "app-af469a92-5b45-4565-b3c4-b79878de67d2";
const roomId: string = "2swovpy1fnunu";

  const res = await sdk.roomV2.createRoom(createRoomParams, appId, roomId);

  if (res.statusCode == 200) {
    // handle response
  }
}

run();
  ```
  ---

  ```json Example Response
  {
    "additionalExposedPorts": [],
    "exposedPort": {
        "port": 6736.6,
        "host": "string",
        "name": "string"
      },
    "roomId": "2swovpy1fnunu"
  }
  ```
  </CH.Code>
</WithStatusBar>
