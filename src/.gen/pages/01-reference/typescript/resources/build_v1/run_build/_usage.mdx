```typescript RunBuild.ts
import { HathoraCloud } from "@hathora/cloud-sdk-typescript";
import { File, RunBuildRequest, RunBuildRequestBody } from "@hathora/cloud-sdk-typescript/dist/models/operations";

async function run() {
  const sdk = new HathoraCloud({
    hathoraDevToken: "",
    appId: "app-af469a92-5b45-4565-b3c4-b79878de67d2",
  });
const requestBody: RunBuildRequestBody = {
  file: {
    content: new TextEncoder().encode("0xcdfCdCbC9d"),
    fileName: "visionary_venezuela.wav",
  },
};
const buildId: number = 1;
const appId: string = "app-af469a92-5b45-4565-b3c4-b79878de67d2";

  const res = await sdk.buildV1.runBuild(requestBody, buildId, appId);

  if (res.statusCode == 200) {
    // handle response
  }
}

run();
```
